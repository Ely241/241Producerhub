import express from 'express';

const router = express.Router();

// Ici, vous pourrez ajouter vos routes spécifiques (par exemple, router.get('/beats', ...))
// Une fois que le débogage de server.ts est terminé et que vous souhaitez refactoriser.

export default router;